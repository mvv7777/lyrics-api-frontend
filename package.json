{
  "name": "api-js-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve -s dist",
    "heroku-postbuild": "npm run build"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
